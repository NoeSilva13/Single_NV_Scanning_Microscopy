# =============================================================================
# Single NV Scanning & ODMR Control Suite - Python dependencies
# =============================================================================
# Install with:  pip install -r requirements.txt
#
# Some hardware SDKs are NOT distributed on PyPI and must be installed
# separately from the vendor (see the "Vendor SDKs" section below and the
# main README.md "Installation" section for download links).
# =============================================================================

# --- Core scientific stack -------------------------------------------------
numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.5.0
scipy>=1.7.0
opencv-python>=4.5.0
tifffile>=2021.11.2

# --- GUI frameworks ----------------------------------------------------------
PySide6>=6.6.0          # Qt6 binding (resolved through qtpy; set QT_API=pyside6)
pyqtgraph>=0.12.0
qtpy>=2.4.0
napari[pyside6]>=0.8.0
magicgui>=0.5.0

# --- Hardware I/O -------------------------------------------------------------
nidaqmx>=0.6.0          # National Instruments USB-6453 DAQ (galvo X/Y + Z piezo via ao2)
pyvisa>=1.11.0           # VISA/Ethernet SCPI control of RIGOL DSG836 (PulseBlaster/rigol_dsg836.py)

# =============================================================================
# Vendor SDKs (NOT installable via pip - install manually, see README.md)
# =============================================================================
# - Swabian Instruments TimeTagger SDK   -> https://www.swabianinstruments.com/time-tagger/downloads/
#   Provides the `TimeTagger` Python module used for photon counting.
# - Swabian Instruments Pulse Streamer   -> pip install pulsestreamer
#   (This one IS on PyPI, listed separately since it's optional if you don't use odmr_gui_qt.py)
# - Player One Astronomy (POA) Camera SDK -> https://player-one-astronomy.com/service/software/
#   Provides the native `PlayerOneCamera` / `ASICamera2` libraries wrapped by Camera/pyPOACamera.py.
# - ZWO ASI Camera SDK (optional)         -> https://astronomy-imaging-camera.com/software-drivers
#   Only required if using the ZWO camera backend in widgets/camera_controls.py.
# - Thorlabs piezo Z-stage: initialize/keep closed-loop with Thorlabs software; this app
#   commands position via DAQ ao2 wired to the controller EXT IN (daq_z_controller.py).

# --- Optional (only needed for odmr_gui_qt.py pulse-sequence control) -------
pulsestreamer>=1.6.0
